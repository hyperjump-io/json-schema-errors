{
  "$schema": "../test-suite.schema.json",

  "description": "The exclusiveMinimum keyword",
  "tests": [
    {
      "description": "exclusiveMinimum",
      "schema": {
        "exclusiveMinimum": 3
      },
      "instance": 3,
      "errors": [
        {
          "messageId": "exclusiveMinimum-message",
          "messageParams": {
            "exclusiveMinimum": "3"
          },
          "instanceLocation": "#",
          "schemaLocations": ["#/exclusiveMinimum"]
        }
      ]
    },
    {
      "description": "exclusiveMinimum pass",
      "schema": {
        "exclusiveMinimum": 3
      },
      "instance": 4,
      "errors": []
    }
  ]
}
